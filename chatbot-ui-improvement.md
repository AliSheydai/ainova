# راهنمای بهبود رابط کاربری چت‌بات تلگرام

> **هدف:** بهبود فرمت پیام‌ها و رابط کاربری بدون تغییر در معماری، منطق تجاری یا ساختار کد فعلی.  
> تمام تغییرات فقط در **محتوای متنی** و **ساختار Keyboard** هستند.

---

## ساختار فعلی چت‌بات

چت‌بات روی **grammY** (کتابخانه تلگرام برای Node.js) پیاده‌سازی شده. پیام‌ها از سه منبع اصلی تولید می‌شوند:

| فایل | نقش |
|---|---|
| `src/lib/telegram/messages.ts` | متن تمام پیام‌های کاربر |
| `src/lib/telegram/keyboards.ts` | دکمه‌های Inline و Reply Keyboard |
| `src/lib/notifications/admin-notification.ts` | پیام‌های اعلان ادمین |
| `src/lib/notifications/user-notification-service.ts` | پیام‌های اعلان کاربر |
| `src/lib/telegram/handlers/orders.ts` | لیست سفارش‌ها (تولید متن پویا) |

---

## مشکلات فعلی

### ۱. ایموجی‌های زیاد و شلوغ‌کننده

در هر خط از پیام‌ها، ایموجی قرار داده شده که باعث می‌شود پیام بصری شلوغ و خسته‌کننده به نظر برسد.

**مثال فعلی:**
```
🧾 **پیش‌فاکتور و تایید سفارش**
━━━━━━━━━━━━━━━━━━━━
📦 **محصول:** گوگل وان
🔹 **پلن:** پایه
🚀 **شیوه تحویل:** لینک آنی
────────────────────
💰 **مبلغ قابل پرداخت:** ۱۵۰,۰۰۰ تومان
🔒 *پرداخت امن در بستر شبکه شتاب...*
```

### ۲. خطوط جداکننده غیرضروری

استفاده از `━━━━━━━━━━━━━━━━━━━━` و `────────────────────` در پیام‌ها، در تلگرام به عنوان متن ساده رندر می‌شود و زشت به نظر می‌رسد.

### ۳. پیام‌های خیلی طولانی

پیام‌هایی مثل `activationGuideOverview`، `formatProductDetails`، و لیست سفارش‌ها بسیار طولانی هستند و کاربر باید زیاد اسکرول کند.

### ۴. استفاده نکردن از ویژگی‌های بومی تلگرام

در بعضی جاها می‌توان از دکمه‌های Inline به جای متن استفاده کرد تا رابط کاربری تمیزتر باشد.

---

## پلن بهبود (گام به گام)

> **مهم:** این تغییرات فقط در محتوای متن و keyboard هستند. هیچ تغییری در منطق handler ها، API ها، دیتابیس یا flow ثبت سفارش ایجاد نمی‌شود.

---

### گام ۱ — بازنویسی پیام‌های اصلی در `messages.ts`

#### اصول کلی فرمت‌بندی جدید:
- فقط **یک ایموجی در ابتدای پیام** (در عنوان)، نه در هر خط
- **حذف کامل خطوط جداکننده** (`━━━` و `────`)
- **پاراگراف‌بندی با خط خالی** به جای جداکننده‌های متنی
- **پیام‌های کوتاه‌تر** — هر پیام فقط اطلاعات ضروری را داشته باشد

---

#### `welcome` — پیام خوشامدگویی

```
// قبل:
سلام ${name} عزیز! 👋
به ربات رسمی فعال‌سازی و خرید **آریوچت** خوش آمدید.

از طریق منوی زیر می‌توانید اشتراک مورد نظر خود را خریداری...

// بعد:
👋 سلام ${name} عزیز!

به ربات آریوچت خوش آمدید.
از منوی پایین گزینه مورد نظر را انتخاب کنید.
```

---

#### `orderSummaryCard` — پیش‌فاکتور سفارش

```
// قبل:
🧾 **پیش‌فاکتور و تایید سفارش**
━━━━━━━━━━━━━━━━━━━━
📦 **محصول:** گوگل وان
🔹 **پلن:** پایه
🚀 **شیوه تحویل:** لینک آنی
────────────────────
💰 **مبلغ قابل پرداخت:** ۱۵۰,۰۰۰ تومان

// بعد:
🧾 **پیش‌فاکتور سفارش**

محصول: ${productTitle}
پلن: ${planName}
شیوه تحویل: ${deliveryLabel}
مبلغ: **${formatPrice(amount)}**

پرداخت از طریق درگاه شاپرک انجام می‌شود.
```

---

#### `paymentSuccess` — پیام موفقیت پرداخت

```
// قبل:
🎉 **پرداخت شما با موفقیت تایید شد!**
📦 **سفارش:** Google One (پایه) (`#AB1234`)
🔐 **بدون نیاز به پسورد یا اطلاعات ورود Google!**
برای فعال‌سازی اشتراک، کافیست روی لینک...
🔗 **لینک اختصاصی شما:**
https://...
💡 این لینک همیشه در بخش «📦 سفارش‌های من» نیز در دسترس شما خواهد بود.

// بعد:
✅ **پرداخت تایید شد**

سفارش ${planName} ثبت شد.
لینک فعال‌سازی اختصاصی شما:

${activationUrl}

لینک در بخش «سفارش‌های من» هم در دسترس است.
```

---

#### `loginPrompt` — درخواست ورود

```
// قبل:
👋 **به ربات رسمی آریوچت خوش آمدید!**
برای دسترسی به امکانات ربات، خرید اشتراک...
📱 **لطفاً شماره موبایل خود را ارسال یا وارد کنید:**
• دکمه «📱 ارسال شماره موبایل» را لمس کنید،
• یا شماره را به صورت مستقیم تایپ کنید (مثال: `۰۹۱۲۳۴۵۶۷۸۹`).

// بعد:
برای ادامه، شماره موبایل خود را وارد کنید.

از دکمه زیر استفاده کنید یا مستقیم تایپ کنید (مثال: `۰۹۱۲۳۴۵۶۷۸۹`).
```

---

#### `activationGuideOverview` — راهنمای فعال‌سازی

```
// قبل (خیلی طولانی):
📖 **راهنمای فعال‌سازی ۲ پلن فروشگاه**
━━━━━━━━━━━━━━━━━━━━
سفارش‌های فروشگاه در قالب **۲ پلن اصلی** تحویل داده می‌شوند:
⚡ **۱. پلن لینک فعال‌سازی آنی (تحویل ۰ ثانیه‌ای)**
• بدون نیاز به پسورد یا اطلاعات ورود گوگل
• دریافت لینک دعوت رسمی اختصاصی بلافاصله پس از پرداخت
...

// بعد (خلاصه، بقیه از طریق دکمه‌ها):
📖 **راهنمای فعال‌سازی**

سفارش‌ها در دو پلن تحویل داده می‌شوند:

**پلن ۱ — لینک آنی:** بدون رمز عبور. لینک دعوت بلافاصله پس از پرداخت تحویل می‌شود.

**پلن ۲ — اکانت روی ایمیل:** شارژ مستقیم روی جیمیل شما یا تحویل اکانت آماده.

برای راهنمای کامل هر پلن، دکمه مربوطه را انتخاب کنید.
```

---

#### `formatProductDetails` — معرفی محصول

```
// قبل (13 خط با ایموجی در هر خط):
✨ **${productName} — ${planName}** ✨
💎 **مزایا و قابلیت‌های شگفت‌انگیز این اشتراک:**
🤖 **Gemini پیشرفته:**
دسترسی به قابلیت‌ها، سرعت و مدل‌های پیشرفته هوش مصنوعی...
🎨 **تولید و ویرایش خلاقانه محتوا:**
...

// بعد (کوتاه و مستقیم):
✨ **${productName} — ${planName}**

اشتراک ${durationMonths} ماهه با دسترسی کامل به Gemini پیشرفته،
Google Workspace AI، فضای ابری Google One و Deep Research.

قیمت: **${formatPrice(price)}**
تحویل: آنی پس از پرداخت
```

---

#### `support` — پشتیبانی

```
// قبل:
🎧 **مرکز پشتیبانی آریوچت**
همکاران ما آماده پاسخگویی به سوالات و راهنمایی شما هستند.
☎️ **شماره تماس:** `09...`
💬 **پشتیبانی تلگرام:** [ارسال پیام در تلگرام](url)
ساعات پاسخگویی: همه‌روزه از ۹ صبح تا ۱۱ شب

// بعد:
🎧 **پشتیبانی آریوچت**

ساعات پاسخگویی: ۹ صبح تا ۱۱ شب

شماره تماس: `${phone}`
```

> دکمه پشتیبانی تلگرام از طریق keyboard نمایش داده می‌شود (هم‌اکنون وجود دارد — نیازی به تغییر نیست).

---

### گام ۲ — بهبود لیست سفارش‌ها در `handlers/orders.ts`

فعلاً هر سفارش با خطوط `────────────────────` از هم جدا می‌شود.

فایل مربوطه: خطوط ۷۷ تا ۱۰۸

```typescript
// قبل:
messageText += `📦 **لیست سفارش‌های شما (تعداد کل: ${totalOrders})**\n`
messageText += `━━━━━━━━━━━━━━━━━━━━\n\n`
...
messageText += `🔢 **سفارش #${orderCode}**\n`
messageText += `📦 **محصول:** ${productTitle}\n`
messageText += `💰 **مبلغ:** ${amount} تومان\n`
messageText += `📅 **تاریخ:** ${dateStr}\n`
messageText += `📊 **وضعیت:** ${getStatusBadge(order.status)}\n\n`
messageText += `\n────────────────────\n\n`

// بعد:
messageText += `📦 **سفارش‌های شما** (${totalOrders} سفارش)\n\n`
...
messageText += `**سفارش #${orderCode}** — ${productTitle}\n`
messageText += `${dateStr} · ${order.amount.toLocaleString('fa-IR')} تومان\n`
messageText += `وضعیت: ${getStatusBadge(order.status)}\n\n`
```

---

### گام ۳ — بهبود keyboard های موجود در `keyboards.ts`

#### `deliveryPreferenceKeyboard` — انتخاب شیوه تحویل
```typescript
// قبل (متن طولانی در دکمه):
'⚡ اکانت آماده انبار (5 عدد - تحویل فوری)'
'👤 فعال‌سازی روی جیمیل شخصی من (۱ الی ۲۴ ساعت)'

// بعد (کوتاه‌تر):
'⚡ اکانت آماده — تحویل فوری'
'👤 فعال‌سازی روی جیمیل من'
```

#### `notificationsListKeyboard` — لیست اعلان‌ها
```typescript
// قبل (فنی و رباتیک):
`✓ خواندن #${i + 1}`

// بعد:
`خواندن اعلان ${i + 1}`
```

---

### گام ۴ — بهبود پیام‌های اعلان

#### `user-notification-service.ts` (خطوط ۱۲۲ تا ۱۲۶)
```typescript
// قبل:
const telegramText = `
${icon} **اعلان جدید:** ${title}
━━━━━━━━━━━━━━━━━━━━
${message}
`.trim()

// بعد:
const telegramText = `${icon} **${title}**\n\n${message}`
```

#### `admin-notification.ts` — پیام سفارش جدید (خطوط ۱۰۵ تا ۱۱۵)
```typescript
// قبل:
`🛍 **ثبت سفارش جدید در سامانه!**\n\n` +
`📦 **سفارش:** #${shortOrderId}\n` +
`🏷 **محصول:** ${productTitle} ${planName}\n` +
`💰 **مبلغ واریزی:** ${this.formatPrice(order.amount)}\n` +
...

// بعد:
`🛍 **سفارش جدید — #${shortOrderId}**\n\n` +
`محصول: ${productTitle} ${planName}\n` +
`مبلغ: ${this.formatPrice(order.amount)}\n` +
`خریدار: ${customerInfo}\n` +
`درگاه: ${order.payment?.gatewayName || 'زرین‌پال'}\n\n` +
`[مشاهده در پنل مدیریت](${appUrl}/dashboard/orders)`
```

---

## خلاصه اصول طراحی پیام

| اصل | وضعیت فعلی | هدف |
|---|---|---|
| تعداد ایموجی در پیام | ۵ تا ۱۵ ایموجی | ۱ تا ۳ (فقط در عنوان) |
| خطوط جداکننده | `━━━` و `────` | حذف کامل |
| طول پیام | خیلی طولانی | کوتاه و مستقیم |
| اطلاعات محصول | ۱۰+ قابلیت در متن | ۲ تا ۳ مزیت اصلی |
| جداکننده سفارش‌ها | `────────────────────` | خط خالی بین سفارش‌ها |
| متن دکمه‌های keyboard | طولانی و پر از ایموجی | کوتاه و اقدام‌محور |

---

## چه چیزی تغییر نمی‌کند

- هیچ handler ای تغییر منطقی نمی‌کند
- هیچ API ای تغییر نمی‌کند
- Flow خرید، OTP، پرداخت کاملاً یکسان می‌مانند
- ساختار Keyboard (کدام دکمه به کدام callback می‌رود) تغییر نمی‌کند
- `parse_mode: 'Markdown'` همچنان استفاده می‌شود

---

## فایل‌هایی که باید ویرایش شوند

| فایل | نوع تغییر |
|---|---|
| `src/lib/telegram/messages.ts` | بازنویسی متن پیام‌ها |
| `src/lib/telegram/keyboards.ts` | کوتاه‌تر کردن متن دکمه‌ها |
| `src/lib/telegram/handlers/orders.ts` | بهبود فرمت لیست سفارش‌ها (خطوط ۷۷–۱۰۸) |
| `src/lib/notifications/admin-notification.ts` | حذف جداکننده‌ها، کاهش ایموجی |
| `src/lib/notifications/user-notification-service.ts` | بهبود فرمت telegramText (خطوط ۱۲۲–۱۲۶) |
