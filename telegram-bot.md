# Master Prompt — توسعه Telegram Bot فروش Google AI Pro

telegram-bot-doc: https://core.telegram.org/bots/api

می‌خواهم بخش Telegram Bot پروژه را توسعه دهی.

هدف اصلی Bot این است که کاربر بتواند مستقیماً از طریق Telegram:

1. محصول Google AI Pro را مشاهده کند.
2. ویژگی‌ها و مزایای آن را ببیند.
3. خرید را انجام دهد.
4. از طریق درگاه پرداخت آنلاین پرداخت کند.
5. پس از پرداخت موفق، لینک فعال‌سازی مربوط به سفارش خود را دریافت کند.
6. سفارش‌ها و فعالیت‌های قبلی خود را مشاهده کند.
7. راهنمای فعال‌سازی و پشتیبانی دریافت کند.

در حال حاضر فقط **یک محصول** داریم:

**Google AI Pro — 18 ماهه**

اما معماری را طوری طراحی کن که در آینده بتوان محصولات و پلن‌های دیگری نیز اضافه کرد و منطق Bot وابسته به یک محصول خاص نباشد.

---

# 1. منوی اصلی Telegram Bot

پس از ورود کاربر به داشبورد Bot، کیبورد اصلی شامل این گزینه‌ها باشد:

* 🛒 خرید Google AI Pro
* 📦 سفارش‌های من
* 📖 راهنمای فعال‌سازی
* 🎧 پشتیبانی

رابط کاربری Bot باید مرتب، حرفه‌ای، کوتاه و قابل فهم باشد.

---

# 2. خرید Google AI Pro

وقتی کاربر روی:

**🛒 خرید Google AI Pro**

کلیک می‌کند، در حال حاضر فقط محصول زیر نمایش داده شود:

### Google AI Pro — 18 ماهه

قبل از نمایش دکمه خرید، یک معرفی جذاب و حرفه‌ای از قابلیت‌های Google AI Pro نمایش بده.

این بخش نباید صرفاً یک متن طولانی باشد؛ قابلیت‌ها را با Emoji، عنوان‌های کوتاه و توضیح مختصر نمایش بده تا در Telegram خوانایی بالایی داشته باشد.

برای مثال:

🤖 **Gemini**
دسترسی به قابلیت‌ها و مدل‌های پیشرفته Gemini مطابق پلن فعلی Google AI Pro.

🎨 **تولید و ویرایش محتوا**
استفاده از قابلیت‌های هوش مصنوعی Google برای تولید متن، تصویر و سایر محتوای پشتیبانی‌شده.

🎬 **Google Flow**
دسترسی به قابلیت‌های AI مرتبط با ساخت و تولید محتوای ویدیویی، در صورت پشتیبانی این قابلیت در پلن و منطقه کاربر.

💻 **AI در ابزارهای Google**
قابلیت‌های هوش مصنوعی در سرویس‌هایی مانند Gmail، Google Docs و سایر سرویس‌های پشتیبانی‌شده.

☁️ **فضای ذخیره‌سازی Google One**
مزایای فضای ذخیره‌سازی و سرویس‌های همراه Google AI Pro مطابق پلن فعلی.

🔬 **Deep Research**
استفاده از قابلیت‌های تحقیق و تحلیل عمیق Gemini، مطابق دسترسی فعلی پلن.

⚡ **دسترسی بیشتر به مدل‌ها و قابلیت‌های پیشرفته**
دسترسی به مدل‌ها و قابلیت‌های پیشرفته‌ای مثل sonnet , Opus که Google برای مشترکان AI Pro ارائه می‌کند.

همچنین اگر قابلیت‌های دیگری در زمان پیاده‌سازی به‌صورت رسمی جزو Google AI Pro هستند، آن‌ها را نیز اضافه کن.

### نکته مهم درباره قابلیت‌ها

اطلاعات قابلیت‌های Google AI Pro ممکن است تغییر کند.

بنابراین:

* قابلیت‌های منسوخ را نمایش نده.
* قابلیت‌هایی که فقط در بعضی کشورها در دسترس هستند را به‌عنوان قابلیت قطعی برای همه کاربران معرفی نکن.
* اگر قابلیت یا نام یک محصول تغییر کرده، از نام فعلی آن استفاده کن.
* از ادعاهای تبلیغاتی غیرواقعی خودداری کن.

هدف این قسمت این است که کاربر در چند ثانیه متوجه شود Google AI Pro چه ارزشی برای او دارد.

---

# 3. دکمه خرید

در انتهای معرفی محصول:

**💳 خرید Google AI Pro — 18 ماهه**

نمایش داده شود.

قیمت باید از تنظیمات/محصول در Backend دریافت شود و Hard-code نشود.

با کلیک روی خرید:

1. یک سفارش برای کاربر ایجاد شود.
2. سفارش در وضعیت Pending قرار بگیرد.
3. اطلاعات سفارش شامل حداقل موارد زیر باشد:

* شناسه سفارش
* شناسه کاربر Telegram
* محصول
* پلن
* مبلغ
* وضعیت پرداخت
* تاریخ ایجاد
* تاریخ پرداخت
* لینک فعال‌سازی تخصیص‌یافته در صورت پرداخت موفق

4. سپس لینک/دکمه ورود به درگاه پرداخت به کاربر نمایش داده شود.

مثلاً:

**سفارش شما ایجاد شد.**

محصول:
Google AI Pro — 18 ماهه

مبلغ:
390000 تومان

برای تکمیل خرید روی دکمه زیر بزنید:

**💳 پرداخت آنلاین**

---

# 4. بعد از پرداخت موفق

پرداخت باید از طریق Backend و Callback/Webhook درگاه تأیید شود.

صرفاً به خاطر بازگشت کاربر از صفحه درگاه، پرداخت را موفق در نظر نگیر.

فقط پس از تأیید واقعی پرداخت توسط Backend:

1. وضعیت سفارش به `paid` / `completed` تغییر کند.
2. یک لینک فعال‌سازی موجود و معتبر از موجودی لینک‌ها به سفارش تخصیص داده شود.
3. لینک به همان سفارش متصل شود.
4. لینک به کاربر نمایش داده شود.
5. سفارش در «سفارش‌های من» قابل مشاهده باشد.

پیام موفقیت:

**✅ پرداخت با موفقیت انجام شد**

سفارش شما ثبت شد.

**Google AI Pro — 18 ماهه**

برای فعال‌سازی، نیازی به ارسال ایمیل، رمز عبور یا اطلاعات ورود حساب Google خود ندارید.

فقط روی لینک زیر بزنید و مراحل فعال‌سازی را با حساب Google خودتان انجام دهید:

**🔗 فعال‌سازی Google AI Pro**

همچنین یک دکمه:

**📖 راهنمای فعال‌سازی**

نمایش داده شود.

---

# 5. مدیریت لینک‌های فعال‌سازی

لینک‌های فعال‌سازی باید از سمت Backend مدیریت شوند و هر لینک دارای وضعیت باشد.

حداقل وضعیت‌های پیشنهادی:

* `available`
* `reserved`
* `used`
* `invalid`

وقتی پرداخت موفق شد:

یک لینک `available` انتخاب و به سفارش تخصیص داده شود.

از تخصیص مجدد یک لینک استفاده‌شده به سفارش دیگر جلوگیری کن.

اگر موجودی لینک تمام شده بود:

* سفارش را به وضعیت مناسب منتقل کن.
* به کاربر وعده تحویل فوری نده.
* پیام مناسبی نمایش بده.
* سفارش و پرداخت را در وضعیت قابل پیگیری نگه دار.
* امکان رسیدگی توسط Admin/Support وجود داشته باشد.

منطق تخصیص لینک باید Transaction-safe باشد تا در صورت خرید همزمان چند کاربر، یک لینک به دو سفارش اختصاص داده نشود.

---

# 6. سفارش‌های من

وقتی کاربر روی:

**📦 سفارش‌های من**

کلیک می‌کند، سابقه سفارش‌های خودش را مشاهده کند.

برای هر سفارش حداقل این اطلاعات نمایش داده شود:

* شماره سفارش
* محصول
* مبلغ
* تاریخ
* وضعیت پرداخت
* وضعیت فعال‌سازی / لینک
* لینک فعال‌سازی در صورت وجود

مثلاً:

**📦 سفارش #10254**

Google AI Pro — 18 ماهه

مبلغ: 1,XXX,XXX تومان

تاریخ: 1405/XX/XX

وضعیت: ✅ پرداخت موفق

🔗 **مشاهده لینک فعال‌سازی**

---

اگر تعداد سفارش‌ها زیاد بود، Pagination داشته باش.

کاربر فقط سفارش‌های متعلق به خودش را ببیند.

همچنین فعالیت‌های مرتبط با خرید او، مانند ایجاد سفارش، پرداخت موفق و دریافت لینک فعال‌سازی، در صورت نیاز در تاریخچه سفارش ثبت شود.

---

# 7. راهنمای فعال‌سازی

گزینه:

**📖 راهنمای فعال‌سازی**

باید یک راهنمای بسیار واضح و ساده ارائه دهد.

محتوا:

### 🔐 نیازی به ارائه حساب Google نیست

برای فعال‌سازی Google AI Pro:

❌ ایمیل و رمز عبور خود را برای ما ارسال نکنید.

❌ اطلاعات ورود حساب Google را در اختیار هیچ شخصی قرار ندهید.

❌ ما نباید رمز عبور حساب شما را دریافت کنیم.

✅ فعال‌سازی از طریق لینک انجام می‌شود.

روند:

1. روی لینک فعال‌سازی دریافت‌شده کلیک کنید.
2. در صورت درخواست Google، با حساب Google خود وارد شوید.
3. مراحل نمایش‌داده‌شده توسط Google را تکمیل کنید.
4. پس از تکمیل مراحل، وضعیت Google AI Pro حساب خود را بررسی کنید.

---

# 8. راهنمای Region

در راهنمای فعال‌سازی یک بخش جداگانه درباره Region قرار بده.

توضیح بده که بعضی قابلیت‌ها و پیشنهادهای Google ممکن است بر اساس کشور/منطقه حساب متفاوت باشند.

به کاربران توصیه کن قبل از فعال‌سازی، Region/کشور حساب Google خود را بررسی کنند و در صورت نیاز، آن را مطابق شرایط واقعی محل استفاده و قوانین Google تنظیم کنند.

از ارائه راهکار برای جعل موقعیت جغرافیایی، دور زدن محدودیت‌های Google یا نقض Terms of Service خودداری کن.

برای مدیریت Country Association حساب Google، لینک رسمی زیر را در قالب دکمه نمایش بده:

https://policies.google.com/country-association-form

متن دکمه:

**🌍 مدیریت کشور/Region حساب Google**

نکته:
این بخش نباید به کاربر تضمین بدهد که با تغییر کشور حتماً تمام قابلیت‌های Google AI Pro در دسترس قرار می‌گیرند.

---

# 9. پشتیبانی

وقتی کاربر روی:

**🎧 پشتیبانی**

کلیک می‌کند، فقط دو گزینه نمایش داده شود:

### ☎️ تماس تلفنی

با کلیک روی آن، شماره تلفن ثابت پشتیبانی نمایش داده شود یا از طریق `tel:` قابل تماس باشد.

شماره تلفن از Configuration دریافت شود و داخل کد Hard-code نشود.

### 💬 پشتیبانی در تلگرام

با کلیک روی آن، PV/Private Chat پشتیبانی باز شود.

آدرس Telegram Support نیز از Configuration دریافت شود.

---

# 10. الزامات مهم امنیتی

این موارد بسیار مهم هستند:

* هیچ‌وقت رمز عبور Google کاربر را دریافت نکن.
* هیچ‌وقت Cookie، Session Token یا اطلاعات حساس حساب Google کاربر را دریافت یا ذخیره نکن.
* Bot نباید از کاربر بخواهد اطلاعات ورود Google را برای ما ارسال کند.
* لینک فعال‌سازی هر سفارش باید فقط برای صاحب همان سفارش قابل مشاهده باشد.
* اطلاعات سفارش کاربران نباید قابل مشاهده برای سایر کاربران باشد.
* Callback پرداخت باید در Backend اعتبارسنجی شود.
* از ثبت چندباره یک پرداخت جلوگیری شود.
* Webhook/Callback باید Idempotent باشد.
* تخصیص لینک باید در شرایط همزمانی امن باشد.
* Secretهای درگاه و سایر Credentialها در Environment Variable باشند.
* لینک‌های فعال‌سازی را در Source Code قرار نده.

---

# 11. معماری قابل توسعه

در حال حاضر فقط:

**Google AI Pro — 18 ماهه**

داریم.

اما از ابتدا سیستم را طوری طراحی کن که بعداً بتوانیم موارد زیر را اضافه کنیم:

* Google AI Pro — 6 ماهه
* Google AI Pro — 12 ماهه
* محصولات دیگر
* پلن‌های مختلف
* قیمت‌های مختلف
* موجودی لینک متفاوت برای هر محصول/پلن

بنابراین از ساختارهایی مثل:

`if product === "google-ai-pro-18-month"`

برای منطق اصلی سیستم استفاده نکن.

محصول، پلن، قیمت، مدت اعتبار و موجودی باید Data-driven باشند.

---

# 12. تجربه کاربری

Bot نباید شلوغ باشد.

اصل طراحی:

**Simple + Fast + Trustworthy**

کاربر باید بتواند در کمترین تعداد کلیک:

خرید → پرداخت → دریافت لینک

را انجام دهد.

از متن‌های بسیار طولانی در Telegram خودداری کن.

اطلاعات را با:

* Emoji
* تیتر
* Bullet
* دکمه‌های Inline Keyboard

مرتب کن.

---

# 13. وضعیت‌های سفارش

حداقل وضعیت‌های زیر را در نظر بگیر:

`pending_payment`

`paid`

`activation_link_assigned`

`completed`

`payment_failed`

`cancelled`

`refunded`

اگر معماری فعلی پروژه وضعیت‌های استاندارد دیگری دارد، با همان سیستم موجود هماهنگ شو و از ایجاد وضعیت‌های موازی غیرضروری خودداری کن.

---

# 14. اصل مهم پیاده‌سازی

قبل از تغییر کد:

1. ساختار فعلی Telegram Bot را بررسی کن.
2. سیستم پرداخت فعلی پروژه را بررسی کن.
3. مدل‌های Database فعلی را بررسی کن.
4. سیستم User/Authentication فعلی را بررسی کن.
5. سیستم ارسال پیام و Keyboard فعلی را بررسی کن.
6. اگر برای Orders، Products، Payments یا Activation Links مدل یا سرویس موجود است، از همان‌ها استفاده کن و Duplicate ایجاد نکن.

بعد از بررسی، این قابلیت را با معماری فعلی پروژه یکپارچه کن.

**در حال حاضر پروژه خام است.**

---

# 15. تست نهایی

پس از پیاده‌سازی حداقل این سناریوها را تست کن:

### سناریو 1

کاربر → خرید → ایجاد سفارش → پرداخت → تأیید پرداخت → تخصیص لینک → نمایش لینک

### سناریو 2

پرداخت ناموفق → عدم تخصیص لینک

### سناریو 3

Callback تکراری → ایجاد نکردن سفارش/تخصیص لینک تکراری

### سناریو 4

دو کاربر همزمان خرید کنند → یک لینک به هر دو اختصاص داده نشود.

### سناریو 5

موجودی لینک تمام شود → سیستم رفتار کنترل‌شده داشته باشد.

### سناریو 6

کاربر سفارش‌های خودش را ببیند و به سفارش کاربر دیگر دسترسی نداشته باشد.

### سناریو 7

کاربر قبل از پرداخت نتواند لینک فعال‌سازی را دریافت کند.

### سناریو 8

کاربر بعد از پرداخت بتواند دوباره لینک مربوط به همان سفارش را از بخش «سفارش‌های من» مشاهده کند.

---

# خروجی مورد انتظار

در پایان:

* Telegram Bot باید آماده فروش Google AI Pro — 18 ماهه باشد.
* خرید از طریق Telegram انجام شود.
* پرداخت از طریق درگاه موجود پروژه انجام شود.
* پرداخت موفق به‌صورت واقعی توسط Backend تأیید شود.
* لینک فعال‌سازی به‌صورت خودکار از موجودی تخصیص داده شود.
* سفارش در Database ثبت شود.
* کاربر بتواند سفارش‌های قبلی خود را مشاهده کند.
* راهنمای فعال‌سازی کامل باشد.
* بخش Region و لینک رسمی Google وجود داشته باشد.
* بخش پشتیبانی شامل تماس تلفنی و Telegram PV باشد.
* معماری برای اضافه‌شدن محصولات و پلن‌های آینده آماده باشد.
* هیچ اطلاعات ورود Google از کاربر دریافت نشود.
* تمام موارد با معماری و Coding Convention فعلی پروژه هماهنگ باشند.
